description: |
  The client handles a failed handshake.

roles:
  primary: client
  participants:
    - server

triggered_by:
  - handshake_alert_received

steps:
  - from: client
    to: server
    capability: connection/missing_capability
    label: Send missing capability
